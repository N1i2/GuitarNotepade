Frontend Priority 1 (сделать обязательно и в первую очередь)

- ✅ Добавить i18n с переключением RU/EN и автовыбором языка. (реализовано: `I18nProvider`, `lib/i18n/messages`, переключатель в шапке, автovыбор из `navigator` + `localStorage`, см. `AIInfo/doneTasks.txt`)

- нужно провести что то типа исправления ошибок(для их исправления надо будет поработать и с беком)
1) i18n переводит не весь текст а только шапку, надо что бы переводился весь текст кроме название и тог что вписал пользователь
2) для аккордов, паттернов, песен и альбомов есть лимиты на создание, надо что бы они отображались(так как сейчас это работает для песен, но если значение -1 то это значит что лимита нет то есть либо премиум либо админ)
3) отсутсвие нужных фильтров
- для аккорда не хватает фильтра для поиска по пальцам(поиск по графическому отображению так как мы создаем аккорд, если не понял о чем я то переспроси) + нету сортировки
- для паттерна вроде есть все что надо, но можешь что нибудь предложить если считаешь что это может быть нужным + нету сортировки 
- для песен, поиск по полному тексту почему то не работает, фильтрация очень сильно рпскидана и не структурирована с точки зрения UI/UX нужно испраить 
- для альбома, тут вроде нормально все пока что 
4) выявлены ошибки при создании песни
- если я хочу посмотреть на то как играется паттерн то мне надо на него нажать и меня должно переносить на него, но меня переносит на страницу и на ней написано что такого паттерна нет, хотя он точно есть 
- при просмотре песни внизу есть комментарии со ссылкой на сегмент где этот комментарий расположен, но эта ссылка не работает, ее нужно либо починить либо убрать

- Перевести хранение JWT в безопасную схему (httpOnly cookie) совместно с backend refresh token.

- Внедрить autosave черновиков песен на backend (не только sessionStorage).

- Нормализовать API-слой:
убрать прямые `fetch` из страниц, оставить единый сервисный доступ.

- Добавить глобальный error boundary и унифицировать обработку ошибок UI.

- Включить CI gate для frontend:
- lint
- typecheck
- tests
